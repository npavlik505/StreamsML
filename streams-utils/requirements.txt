'''Soon to be local system dependencies installed by user in venv'''
# VENV: pip, setuptools, wheel, rust, go, just, apptainer
'''
apt-get update -y
apt-get install curl \
	libssl-dev \
	pkg-config \
	git \
	python3 \
	python3-dev \
	python3-pip \
	libhdf5-mpi-dev \
	build-essential \
	libopenmpi-dev -y
'''
# numpy, mpi4py, h5py, numpy, python-sensors, pydmd, control


